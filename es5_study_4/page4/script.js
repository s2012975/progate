// 関数attackの引数にcharacterを追加してください
function attack(item, character) {
  // 「△△は○○を使って攻撃した！」とコンソールに出力してください
  console.log( character + "は" + item +"を使って攻撃した！");
  
}

// 関数attackの引数に「手裏剣」と「ベイビーわんこ」を渡して、関数を呼び出してください
attack("手裏剣", "ベイビーわんこ");
